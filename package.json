{
  "name": "listagem-usuarios",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicacao React para listagem de usuarios com Parcel.",
  "scripts": {
    "start": "npm run build && node scripts/serve-dist.mjs",
    "build": "parcel build --target web --no-cache",
    "test": "echo \"Error: no test specified\""
  },
  "dependencies": {
    "axios": "^1.20.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "parcel": "^2.16.4"
  },
  "type": "module",
  "engines": {
    "node": ">=20 <24"
  },
  "targets": {
    "web": {
      "context": "browser",
      "source": "index.html",
      "distDir": "dist"
    }
  },
  "license": "ISC",
  "keywords": [],
  "author": ""
}
